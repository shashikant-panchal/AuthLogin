<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authorize User</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>Authorize Login</h1>
        <p>User ID: <%= userId %></p>
        <!-- Redirect to user.ejs with userId as query parameter -->
        <form action="/authorize/redirect/<%= userId %>" method="GET">
            <button type="submit">Authorize</button>
        </form>
    </div>
</body>
</html>
